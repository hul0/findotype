[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "findotype"
version = "1.0.0"
description = "Offline Disease Ontology and Human Phenotype Ontology engine and search backend"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "AGPL-3.0-or-later" }
authors = [
    { name = "Rupam Ghosh", email = "hulo@crine.in" }
]
keywords = [
    "disease-ontology",
    "doid",
    "human-phenotype-ontology",
    "hpo",
    "ontology",
    "sqlite",
    "fts5",
    "medical-informatics",
    "biomedical"
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Healthcare Industry",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Medical Science Apps."
]
dependencies = []

[project.urls]
Homepage = "https://github.com/hul0/findotype"
Repository = "https://github.com/hul0/findotype"
Documentation = "https://github.com/hul0/findotype#readme"
"Bug Tracker" = "https://github.com/hul0/findotype/issues"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0"
]

[project.scripts]
findotype = "findotype.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
